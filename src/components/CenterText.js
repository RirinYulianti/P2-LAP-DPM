import React from 'react';
import { Text, View } from 'react-native';
import styles from '../styles/CenterTextStyle';

const CenterText = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>RIRIN JULIYANTI</Text>
    </View>
  );
};

export default CenterText;
